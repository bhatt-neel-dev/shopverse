server {
  listen 9001;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://studio-api:9000/;
    proxy_http_version 1.1;
    proxy_read_timeout 300s; # bulk trace runs can take minutes
  }

  location / {
    try_files $uri /index.html;
  }
}
